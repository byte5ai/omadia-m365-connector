# The @omadia/* peers are private workspace packages (not on npm) — they are
# provided by the Omadia host at runtime, never installed here. Skip npm's
# peer auto-install so `npm install` doesn't 404 trying to fetch them.
legacy-peer-deps=true
